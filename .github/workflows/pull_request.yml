name: Pull request checks

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

# Dette repoet har ingen byggsteg, men org-regelsettet main-branch-protection
# krever en "build"-statussjekk pa alle PR-er. Denne gate-jobben rapporterer
# derfor alltid en bestatt build, slik at PR-er ikke last i evig pending.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ingen byggsteg kreves for dette repoet"
